# Runtime
NODE_ENV=production
PORT=3000

# Public app URL used in QR/session links (must be HTTPS in production)
PUBLIC_BASE_URL=https://your-app.onrender.com

# Optional localtunnel behavior for local dev only
AUTO_TUNNEL=false

# Google Vision credentials:
# Option A (recommended on hosting platforms): mount service account JSON as secret file
# and set GOOGLE_APPLICATION_CREDENTIALS to that path.
# GOOGLE_APPLICATION_CREDENTIALS=/etc/secrets/gcp-vision-key.json

# Option B: keep GOOGLE_APPLICATION_CREDENTIALS unset and use local vision-key.json (local dev only)
